<%= link_to "Cancel", region_path %>

<h3>Edit this region:</h3>

<% if flash[:errors] %>
  <% flash[:errors].each do |error| %>
    <li> <%= error %> </li>
  <% end %>
<% end %>

<%= render "form" %>
<br></br>
<%= form_tag region_path(@region), method: "delete" do %>
<%= submit_tag "Delete" %>
<% end %>
